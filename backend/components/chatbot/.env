# Port for your Express server to listen on
PORT=8000

# Path to your JSON file containing Q&A data
JSON_PATH=./data/medico_chatbot_2000_qa.json

# Similarity threshold for fuzzy matching (lower = stricter, 0.0 = exact match)
FUSE_THRESHOLD=0.6
